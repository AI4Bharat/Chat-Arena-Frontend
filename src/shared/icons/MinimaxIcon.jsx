import React from 'react';

const MinimaxIcon = ({ className, ...props }) => {
  return <img className={className} src="/minimax-logo.png" alt="Minimax Icon" {...props} />;
};

export default MinimaxIcon;
