import React from 'react';

const CartesiaIcon = ({ className, ...props }) => {
  return <img className={className} src="/cartesia-logo.svg" alt="Cartesia Icon" {...props} />;
};

export default CartesiaIcon;
