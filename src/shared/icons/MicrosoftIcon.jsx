import React from 'react';

const MicrosoftIcon = ({ className, ...props }) => {
  return <img className={className} src="/microsoft.svg" alt="Microsoft Icon" {...props} />;
};

export default MicrosoftIcon;
